<Window x:Class="Converki.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Converki"
        mc:Ignorable="d"
        Title="Convertki" 
        Height="450" 
        Width="800">
    <Grid Background="LightBlue">
        <TextBox HorizontalAlignment="Left" Margin="10,10,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120" TextChanged="TextBox_TextChanged" Text="변환할 대상을 입력"/>
        
        <Button  
            Background="DarkCyan"
            Padding="10, 10, 10, 10"
            BorderBrush="Black"
            BorderThickness="1"
            HorizontalAlignment="Right" 
            VerticalAlignment="Top" 
            Margin="10, 10, 10, 10" Click="Button_Click">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="15"/>
                </Style>
            </Button.Resources>
            <StackPanel 
                Orientation="Horizontal">
                <Image Source="overflow_white.png" Height="10" Width="10" Margin="0, 0, 10, 0"/>
                <TextBlock Text="기록" Foreground="White"/>
            </StackPanel>
        </Button>

    </Grid>
</Window>
